{
  "name": "task-manager-app-assignment",
  "private": true,
  "version": "1.0.0",
  "description": "Simple full-stack task manager",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
